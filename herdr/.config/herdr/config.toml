# Herdr config — synced via stow. Only this file is synced; session.json,
# logs, and sockets stay local in ~/.config/herdr/ (the rest are runtime
# files generated by herdr itself).

[keys]
prefix = "ctrl+a"